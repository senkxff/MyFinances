<UserControl x:Class="MyFinances.UserControls.BlueBackgroundButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MyFinances.UserControls"
             mc:Ignorable="d" 
             Height="30" Width="100">

    <UserControl.Resources>
        <FontFamily x:Key="Comic">pack://application:,,,/View/Fonts/Comic.ttf#Comic Sans MS</FontFamily>
    </UserControl.Resources>

    <Grid>
        <Button Content="закрыть" Width="100" Height="30" FontFamily="Comic Sans MS" Foreground="Wheat" MouseLeftButtonDown="Button_MouseLeftButtonDown">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" 
                    Background="Blue" 
                    BorderBrush="DarkBlue" 
                    BorderThickness="1" 
                    CornerRadius="5">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="DarkBlue"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="DarkBlue"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
        </Button>
    </Grid>
</UserControl>
